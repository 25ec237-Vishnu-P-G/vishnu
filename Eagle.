#include &lt;stdio.h>

int main() {
    int num1, num2, sum;
    printf("Enter two integers: ");
    scanf("%d %d", &amp;num1, &amp;num2);
    sum = num1 + num2;
    printf("The sum is: %d\n", sum);
    return 0;
}

